{% extends 'base.html.twig' %}

{% block body %}
<section class = "row articles">
{% for item in produits %}
    

<article class="col-md-4 mx-auto text-center border border-dark mb-3 p-3 mt-3 mr-1"
<h2>{{ item.titre }}</h2>


<div class="metadata">Ecrit{{ item.createdAt|date('d/m/Y à H:i:s') }}
 
</div>
<div class="content">
<img class="img-fluid" src="{{ item.photo }}" alt="">
{{ item.description|raw }}
  {# <a href="{{ path('produit_show',{id:item.id})}}" class="btn btn-primary">lire la suite</a>  <br><br> #}
 <a href="{{path('cart_add',{id:item.id})}} " class="card-link btn btn-primary">Ajouter au panier</a>
</div>

</article>
{% endfor %}
</section>


{% endblock %}
